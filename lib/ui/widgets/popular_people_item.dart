import 'package:flutter/material.dart';

class PopularPeopleItem extends StatelessWidget {
  const PopularPeopleItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: () {

      },

    );
  }
}
